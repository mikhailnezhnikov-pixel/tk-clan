name: Deploy Userscript 1.17.32 Clan Shop History Parser

on:
  push:
    paths:
      - 'deploy/topking/userscript-1-17-32-clan-shop-history-trigger.txt'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-topking-userscript
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.TOPKING_SSH_KEY }}
        run: |
          set -euo pipefail
          install -d -m 700 "$HOME/.ssh"
          printf '%s\n' "$SSH_PRIVATE_KEY" > "$HOME/.ssh/topking"
          chmod 600 "$HOME/.ssh/topking"

      - name: Fetch and patch live userscript
        run: |
          set -euo pipefail
          scp -q -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new root@89.125.1.71:/opt/hamsterking-license/HamsterKingMobile.user.js /tmp/HamsterKingMobile.user.js
          grep -Fq "// @version      1.17.31" /tmp/HamsterKingMobile.user.js
          python3 deploy/topking/patch_clan_shop_purchase_history_parser_1_17_32.py
          node --check /tmp/HamsterKingMobile.user.js
          grep -Fq "// @version      1.17.32" /tmp/HamsterKingMobile.user.js
          grep -Fq "clan-shop-purchase-history-parser-20260922-r2" /tmp/HamsterKingMobile.user.js
          grep -Fq "/clan-shop-events/submit" /tmp/HamsterKingMobile.user.js

      - name: Deploy userscript
        run: |
          set -euo pipefail
          scp -q -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new /tmp/HamsterKingMobile.user.js root@89.125.1.71:/tmp/HamsterKingMobile.1.17.32.user.js
          ssh -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new root@89.125.1.71 '
            set -euo pipefail
            stamp="$(date +%Y%m%d-%H%M%S)"
            node --check /tmp/HamsterKingMobile.1.17.32.user.js
            cp -a /opt/hamsterking-license/HamsterKingMobile.user.js "/opt/hamsterking-license/HamsterKingMobile.user.js.bak.1.17.32.$stamp"
            install -m 0644 /tmp/HamsterKingMobile.1.17.32.user.js /opt/hamsterking-license/HamsterKingMobile.user.js
            printf "%s\n" "[Service]" "Environment=\"HK_MIN_SCRIPT_VERSION=1.17.32\"" > /etc/systemd/system/hamsterking-license.service.d/min-version.conf
            systemctl daemon-reload
            systemctl restart hamsterking-license.service
            systemctl is-active --quiet hamsterking-license.service
          '

      - name: Verify public script
        run: |
          set -euo pipefail
          curl -fsSL --retry 4 --retry-delay 2 "https://hk-license.89.125.1.71.sslip.io/panel.js?clan-shop-history=$(date +%s)" -o /tmp/public.js
          node --check /tmp/public.js
          cmp -s /tmp/HamsterKingMobile.user.js /tmp/public.js
          grep -Fq "// @version      1.17.32" /tmp/public.js
          grep -Fq "clan-shop-purchase-history-parser-20260922-r2" /tmp/public.js
          echo "PUBLIC_1_17_25=PASS"

      - name: Sync baseline
        run: |
          set -euo pipefail
          cp /tmp/HamsterKingMobile.user.js baseline/topking/HamsterKingMobile.current.user.js
          printf "%s\n" "USERSCRIPT_1_17_32_CLAN_SHOP_HISTORY_PARSER=PASS" "version=1.17.32" > audit/hk-userscript-1-17-32-clan-shop-history-parser-status.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add baseline/topking/HamsterKingMobile.current.user.js audit/hk-userscript-1-17-32-clan-shop-history-parser-status.txt
          if ! git diff --cached --quiet; then
            git commit -m "fix: parse Clan Shop history in userscript 1.17.32"
            git pull --rebase origin main
            git push origin HEAD:main
          fi

      - name: Cleanup
        if: always()
        run: rm -f "$HOME/.ssh/topking" /tmp/HamsterKingMobile.user.js /tmp/public.js
