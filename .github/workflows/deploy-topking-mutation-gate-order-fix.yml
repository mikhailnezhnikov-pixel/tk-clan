name: Deploy TopKing 1.17.4 Mutation Gate Order Fix

on:
  push:
    paths:
      - 'deploy/topking/mutation-gate-order-fix-trigger.txt'
  workflow_dispatch:

concurrency:
  group: deploy-topking-userscript
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4

      - name: Prepare SSH key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.TOPKING_SSH_KEY }}
        run: |
          set -euo pipefail
          install -d -m 700 "$HOME/.ssh"
          printf '%s\n' "$SSH_PRIVATE_KEY" > "$HOME/.ssh/topking"
          chmod 600 "$HOME/.ssh/topking"

      - name: Fetch live
        run: |
          set -euo pipefail
          scp -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new \
            root@89.125.1.71:/opt/hamsterking-license/HamsterKingMobile.user.js /tmp/HamsterKingMobile.user.js
          grep -q '^// @version      1.17.4$' /tmp/HamsterKingMobile.user.js
          grep -Fq "HK_STARTUP_ERROR_TRAP_REV = 'startup-error-trap-20260920-r8'" /tmp/HamsterKingMobile.user.js
          node --check /tmp/HamsterKingMobile.user.js

      - name: Apply mutation gate order fix
        run: |
          set -euo pipefail
          python3 deploy/topking/patch_mutation_gate_order_1174.py
          grep -Fq "HK_MUTATION_GATE_ORDER_REV = 'mutation-gate-order-20260920-r1'" /tmp/HamsterKingMobile.user.js
          python3 - <<'PY'
          from pathlib import Path
          s=Path('/tmp/HamsterKingMobile.user.js').read_text()
          d=s.find('const hkMutationGate = (() => {')
          r=s.find('runtime.mutationGate = hkMutationGate;')
          a=s.find("async function apiJson(path, method = 'GET'", d)
          print('ORDER', d, r, a)
          assert d >= 0 and d < r < a
          assert s.count('runtime.mutationGate = hkMutationGate;') == 1
          PY
          node --check /tmp/HamsterKingMobile.user.js

      - name: Deploy
        run: |
          set -euo pipefail
          scp -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new \
            /tmp/HamsterKingMobile.user.js root@89.125.1.71:/tmp/HamsterKingMobile.user.js
          ssh -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new root@89.125.1.71 'set -euo pipefail;
            stamp="$(date +%Y%m%d-%H%M%S)";
            node --check /tmp/HamsterKingMobile.user.js;
            cp -a /opt/hamsterking-license/HamsterKingMobile.user.js "/opt/hamsterking-license/HamsterKingMobile.user.js.bak.$stamp";
            install -m 0644 /tmp/HamsterKingMobile.user.js /opt/hamsterking-license/HamsterKingMobile.user.js;
            systemctl restart hamsterking-license.service;
            systemctl is-active --quiet hamsterking-license.service;
            grep -Fq "HK_MUTATION_GATE_ORDER_REV = '\''mutation-gate-order-20260920-r1'\''" /opt/hamsterking-license/HamsterKingMobile.user.js;
            node --check /opt/hamsterking-license/HamsterKingMobile.user.js'

      - name: Verify public and order
        run: |
          set -euo pipefail
          curl -fsSL --retry 2 --connect-timeout 15 "https://hk-license.89.125.1.71.sslip.io/panel.js?v=$(date +%s)" -o /tmp/public.js
          cmp -s /tmp/HamsterKingMobile.user.js /tmp/public.js
          grep -Fq "HK_MUTATION_GATE_ORDER_REV = 'mutation-gate-order-20260920-r1'" /tmp/public.js
          python3 - <<'PY'
          from pathlib import Path
          s=Path('/tmp/public.js').read_text()
          d=s.find('const hkMutationGate = (() => {')
          r=s.find('runtime.mutationGate = hkMutationGate;')
          a=s.find("async function apiJson(path, method = 'GET'", d)
          print('PUBLIC_ORDER', d, r, a)
          assert d >= 0 and d < r < a
          assert s.count('runtime.mutationGate = hkMutationGate;') == 1
          PY
          node --check /tmp/public.js
          echo "PUBLIC_SHA=$(sha256sum /tmp/public.js | awk '{print $1}')"

      - name: Cleanup
        if: always()
        run: rm -f "$HOME/.ssh/topking" /tmp/HamsterKingMobile.user.js /tmp/public.js
