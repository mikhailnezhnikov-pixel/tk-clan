name: Deploy TopKing Explore E2 R6 Fresh Meta

on:
  push:
    paths:
      - 'deploy/topking/explore-e2-r6-fresh-meta-trigger.txt'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: deploy-topking-userscript
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Prepare SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.TOPKING_SSH_KEY }}
        run: |
          set -euo pipefail
          install -d -m 700 "$HOME/.ssh"
          printf '%s\n' "$SSH_PRIVATE_KEY" > "$HOME/.ssh/topking"
          chmod 600 "$HOME/.ssh/topking"

      - name: Fetch current live and patch Explore only
        run: |
          set -euo pipefail
          scp -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new root@89.125.1.71:/opt/hamsterking-license/HamsterKingMobile.user.js /tmp/HamsterKingMobile.user.js
          node --check /tmp/HamsterKingMobile.user.js
          grep -Fq "HK_EXPLORE_CANON_REV='explore-readonly-plan-20260920-r5-filters'" /tmp/HamsterKingMobile.user.js
          grep -Fq "const HK_MAP_READ_CONCURRENCY = 5;" /tmp/HamsterKingMobile.user.js
          sha256sum /tmp/HamsterKingMobile.user.js | awk '{print $1}' > /tmp/source.sha
          cp /tmp/HamsterKingMobile.user.js /tmp/before.js

          python3 deploy/topking/patch_explore_e2_r6_fresh_meta.py
          node --check /tmp/HamsterKingMobile.user.js

          grep -Fq "HK_EXPLORE_CANON_REV='explore-readonly-plan-20260920-r6-fresh-meta'" /tmp/HamsterKingMobile.user.js
          grep -Fq "function exploreBuildingType(...src)" /tmp/HamsterKingMobile.user.js
          grep -Fq "const HK_MAP_READ_CONCURRENCY = 5;" /tmp/HamsterKingMobile.user.js

          python3 - <<'PY'
          from pathlib import Path
          before=Path('/tmp/before.js').read_text()
          after=Path('/tmp/HamsterKingMobile.user.js').read_text()
          def bounds(s):
              a=s.index("  const HK_EXPLORE_CANON_REV=")
              b=s.index("  async function acceptBuildingStudy(",a)
              return a,b
          ba,bb=bounds(before)
          aa,ab=bounds(after)
          assert before[:ba]==after[:aa]
          assert before[bb:]==after[ab:]
          block=after[aa:ab]
          assert "explore-readonly-plan-20260920-r6-fresh-meta" in block
          assert "function exploreReadFreshPlayer" in block
          assert "function exploreBuildingType(...src)" in block
          assert "Number(total)>20?'normal':'investment'" in block
          assert "invest_building_list" not in block
          forbidden=[
              "/player/building/fast_completion",
              "/player/battle/fast",
              "/player/building/remort?",
              "/player/building/fast_remort",
              "/shop/buy",
          ]
          assert not [x for x in forbidden if x in block]
          PY
          sha256sum /tmp/HamsterKingMobile.user.js | awk '{print $1}' > /tmp/candidate.sha

      - name: Race-safe install
        run: |
          set -euo pipefail
          source_sha="$(cat /tmp/source.sha)"
          scp -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new /tmp/HamsterKingMobile.user.js root@89.125.1.71:/tmp/HamsterKingMobile.explore-e2-r6.user.js
          ssh -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new root@89.125.1.71 "set -euo pipefail; current=\$(sha256sum /opt/hamsterking-license/HamsterKingMobile.user.js | awk '{print \$1}'); test \"\$current\" = \"$source_sha\" || { echo LIVE_CHANGED_BEFORE_EXPLORE_E2_R6; exit 42; }; node --check /tmp/HamsterKingMobile.explore-e2-r6.user.js; stamp=\$(date +%Y%m%d-%H%M%S); cp -a /opt/hamsterking-license/HamsterKingMobile.user.js /opt/hamsterking-license/HamsterKingMobile.user.js.bak.explore-e2-r6.\$stamp; install -m 0644 /tmp/HamsterKingMobile.explore-e2-r6.user.js /opt/hamsterking-license/HamsterKingMobile.user.js; systemctl restart hamsterking-license.service; systemctl is-active --quiet hamsterking-license.service"

      - name: Verify public exact candidate
        run: |
          set -euo pipefail
          sleep 2
          curl -fsSL --retry 4 --connect-timeout 15 --max-time 30 "https://hk-license.89.125.1.71.sslip.io/panel.js?explore-e2-r6=$(date +%s)" -o /tmp/public.js
          node --check /tmp/public.js
          cmp -s /tmp/HamsterKingMobile.user.js /tmp/public.js
          candidate="$(cat /tmp/candidate.sha)"
          test "$(sha256sum /tmp/public.js | awk '{print $1}')" = "$candidate"
          grep -Fq "HK_EXPLORE_CANON_REV='explore-readonly-plan-20260920-r6-fresh-meta'" /tmp/public.js
          grep -Fq "const HK_MAP_READ_CONCURRENCY = 5;" /tmp/public.js
          python3 - <<'PY'
          from pathlib import Path
          s=Path('/tmp/public.js').read_text()
          a=s.index("  const HK_EXPLORE_CANON_REV=")
          b=s.index("  async function acceptBuildingStudy(",a)
          x=s[a:b]
          forbidden=[
              "/player/building/fast_completion",
              "/player/battle/fast",
              "/player/building/remort?",
              "/player/building/fast_remort",
              "/shop/buy",
          ]
          assert not [v for v in forbidden if v in x]
          assert "function exploreReadFreshPlayer" in x
          assert "Number(total)>20?'normal':'investment'" in x
          assert "invest_building_list" not in x
          PY
          {
            echo "EXPLORE_E2_R6_FRESH_META_LIVE=PASS"
            echo "source_live_sha256=$(cat /tmp/source.sha)"
            echo "live_sha256=$candidate"
            echo "public_sha256=$candidate"
            echo "public_byte_equality=PASS"
            echo "syntax=PASS"
            echo "mutation_endpoints_in_explore=0"
            echo "fresh_player_state=PASS"
            echo "building_type_filter=donor_threshold_exact"
            echo "normal_rule=total_events_gt_20"
            echo "investment_rule=total_events_le_20"
            echo "map_concurrency_5_preserved=PASS"
            echo "maps_backend_schema_changed=NO"
          } > /tmp/explore-e2-r6-live-status.txt

      - name: Sync baseline and evidence
        run: |
          set -euo pipefail
          pushed=0
          for attempt in 1 2 3; do
            git fetch origin main
            git reset --hard origin/main
            cp /tmp/HamsterKingMobile.user.js baseline/topking/HamsterKingMobile.current.user.js
            cp /tmp/explore-e2-r6-live-status.txt audit/hk-stage2-explore-e2-r6-fresh-meta-live-status.txt
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add baseline/topking/HamsterKingMobile.current.user.js audit/hk-stage2-explore-e2-r6-fresh-meta-live-status.txt
            git commit -m "stage2: fix Explore fresh state and type filters r6" || true
            if git push origin HEAD:main; then pushed=1; break; fi
            sleep 2
          done
          test "$pushed" = 1

      - name: Cleanup
        if: always()
        run: rm -f "$HOME/.ssh/topking" /tmp/HamsterKingMobile.user.js /tmp/public.js /tmp/before.js /tmp/source.sha /tmp/candidate.sha /tmp/explore-e2-r6-live-status.txt
