name: Verify Public Collector Token Refresh

on:
  push:
    paths:
      - 'deploy/topking/public-collector-token-refresh-trigger.txt'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4

      - name: Prepare SSH
        env:
          SSH_PRIVATE_KEY: ${{ secrets.TOPKING_SSH_KEY }}
        run: |
          set -euo pipefail
          install -d -m 700 "$HOME/.ssh"
          printf '%s\n' "$SSH_PRIVATE_KEY" > "$HOME/.ssh/topking"
          chmod 600 "$HOME/.ssh/topking"

      - name: Force one safe server-side collector refresh
        run: |
          set -euo pipefail
          python3 -m py_compile deploy/topking/check_public_collector_token_refresh.py
          scp -q -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new \
            deploy/topking/check_public_collector_token_refresh.py \
            root@89.125.1.71:/tmp/check_public_collector_token_refresh.py
          ssh -i "$HOME/.ssh/topking" -o BatchMode=yes -o ConnectTimeout=15 -o StrictHostKeyChecking=accept-new \
            root@89.125.1.71 'python3 /tmp/check_public_collector_token_refresh.py'

      - name: Cleanup
        if: always()
        run: rm -f "$HOME/.ssh/topking"
